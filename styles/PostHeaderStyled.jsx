import styled from 'styled-components';
import { Container } from '@zeal-ui/core';

const PostHeaderStyled = styled(Container)`
	margin-bottom: 2rem;

	.postCategory {
		margin: 0rem 0.5rem;
	}
`;

export default PostHeaderStyled;
